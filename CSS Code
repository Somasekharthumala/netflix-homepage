/* Reset some default styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial, sans-serif;
}

/* Styling for header, navigation, and main sections */
header {
    background-color: #111;
    color: #fff;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
}

.logo img {
    width: 100px;
}

nav ul {
    list-style: none;
    display: flex;
}

nav li {
    margin: 0 15px;
    cursor: pointer;
}

main {
    background-image: url('background-image.jpg');
    background-size: cover;
}

/* Styling for hero section */
.hero {
    background-color: rgba(0, 0, 0, 0.7);
    padding: 100px 0;
    text-align: center;
    color: #fff;
}

.hero-content h1 {
    font-size: 2.5em;
    margin-bottom: 20px;
}

.hero-content p {
    font-size: 1.2em;
    margin-bottom: 20px;
}

/* Styling for featured section */
.featured {
    background-color: #222;
    color: #fff;
    padding: 20px;
}

.featured h2 {
    font-size: 1.5em;
    margin-bottom: 20px;
}

/* Styling for footer */
footer {
    background-color: #111;
    color: #fff;
    text-align: center;
    padding: 10px;
}

/* Add more CSS to style your featured movie/show cards and other elements as needed. */

